import { Manrope, Nunito, Montserrat } from "next/font/google";

export const euclide = Manrope({
  variable: "--font-euclide",
  subsets: ["latin"],
});

export const nunito = Nunito({
  variable: "--font-nunito",
  subsets: ["latin"],
});

export const montserrat = Montserrat({
  variable: "--font-montserrat",
  subsets: ["latin"],
});
